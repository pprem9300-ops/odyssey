/* ============================================================================
   ODYSSEY — CLOUD CONFIG  (optional, 100% free)
   ----------------------------------------------------------------------------
   Leave these BLANK to run in local-only mode (on-device, no account).
   To enable free cross-device sync (iPhone <-> MacBook):

     1. Create a FREE Supabase project at https://supabase.com  (no credit card).
     2. SQL Editor -> run the snippet in PROJECT_STATUS.md §10 (creates the table).
     3. Settings -> API -> copy "Project URL" and the "anon public" key below.
     4. Authentication -> Providers -> Email is on by default (magic link).

   The anon key is SAFE to expose in front-end code — it only works through
   Row-Level Security, so each user can read/write ONLY their own row.
   ========================================================================== */
export const SUPABASE_URL = 'https://jrlwvadwlwsljdtxpwrb.supabase.co';
export const SUPABASE_ANON_KEY = 'sb_publishable_Z8KO1Q46FLW1kKbF75rPgw_Usq_9bmX';
